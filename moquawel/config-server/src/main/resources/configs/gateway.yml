server:
  port: 8888
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: authentication
          uri: lb:http://AUTHENTICATION
          predicates:
            - Path=/api/v1/auth/**

